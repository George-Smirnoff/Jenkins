#!groovy

properties([disableConcurrentBuilds])

pipeline {
    agent {
        label 'master'
    }
    options {
        buildDiscarder(logRotator(NumKeepStr: '10', artifactsNumKeepStr: '10'))
        timestamps()
    }
    stages {
        stage("First Step") {
            steps {
                sh 'ssh root@192.168.117.186' \'ip\''
            }
        }
    }
}